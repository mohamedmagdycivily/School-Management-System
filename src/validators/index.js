const authValidator = require('./auth.validator');
const schoolValidator = require('./school.validator');
const classroomValidator = require('./classroom.validator');
const studentValidator = require('./student.validator');

module.exports = {
  authValidator,
  schoolValidator,
  classroomValidator,
  studentValidator,
};
